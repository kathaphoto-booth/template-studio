---
name: loom-auditor
description: Enforces Wabi-Sabi baseline and woven silk overlay rules
---

# Loom Auditor

## Directives
1. Verify the Woven Silk Overlay Rule (BRAND_GENESIS_PLAN §VIII): Decorative SVG threads must intrude into photo margins by 12-16px.
2. Ensure threads that overlay images (`z-10` or higher) use `pointer-events-none` so they do not block interactions.
3. Check for deckled edges (`DeckledCard`) around artworks instead of sharp geometric borders.
4. Forbid legacy `oax` tokens anywhere in the codebase.
