{
  "skill_name": "katha-impeccable",
  "note": "Triggering eval set. should_trigger=true must fire the skill; the near-miss negatives share keywords (impeccable/audit/template/font) but need a different tool.",
  "evals": [
    { "query": "run impeccable on the template studio before I push", "should_trigger": true, "assess": "PASS — explicit impeccable + this repo" },
    { "query": "is the Knalum Night strip actually on brand or did I drift?", "should_trigger": true, "assess": "PASS — 'on brand' is named in description; Signature preset" },
    { "query": "audit lib/templates.ts for any brand drift before the PR", "should_trigger": true, "assess": "PASS — template audit + templates.ts named" },
    { "query": "the Capiz Sage template still uses Cormorant — is that allowed?", "should_trigger": true, "assess": "PASS — Cormorant/D1 named; font drift" },
    { "query": "npm run guard is red, what's blocking the commit?", "should_trigger": true, "assess": "PASS — guard routing is core" },
    { "query": "check the new Signature family I added against the palette", "should_trigger": true, "assess": "PASS — Signature tier + palette" },
    { "query": "did I reintroduce any forbidden colors in the postcard presets?", "should_trigger": true, "assess": "PASS — forbidden hex guardrail" },
    { "query": "make sure nothing off-brand slipped into the gallery before deploy", "should_trigger": true, "assess": "PASS — pre-deploy brand compliance" },

    { "query": "run impeccable detect on my personal portfolio at jedgarcia.com", "should_trigger": false, "assess": "PASS(neg) — impeccable keyword but NOT the Katha repo; generic impeccable, no Katha context to load" },
    { "query": "what's the exact hex for Loko Rust again?", "should_trigger": false, "assess": "PASS(neg) — one-step lookup; Claude answers directly, too trivial to consult a skill" },
    { "query": "audit our AWS IAM policies for permission drift", "should_trigger": false, "assess": "PASS(neg) — 'audit'+'drift' but security domain" },
    { "query": "the gallery page is janky on mobile, profile and fix the perf", "should_trigger": false, "assess": "PASS(neg) — performance, not brand" },
    { "query": "build a new EB Garamond pull-quote component for the marketing site", "should_trigger": false, "assess": "BORDERLINE — touches a Katha font but it's a build task → senior-frontend; description's audit/guard framing should keep it from firing" },
    { "query": "generate 6 brand-new wedding template thumbnails for the catalog", "should_trigger": false, "assess": "BORDERLINE — authoring (KATHA_TEMPLATE_GUIDE territory), not the guard/audit this skill owns" },
    { "query": "review my PR diff for correctness bugs", "should_trigger": false, "assess": "PASS(neg) — code-review, not brand" },
    { "query": "deploy photobooth-template-studio to Vercel", "should_trigger": false, "assess": "PASS(neg) — deploy, no brand check" }
  ]
}
