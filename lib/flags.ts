// A1 legal gate (spec r1.1 A1): the intake contract-terms line carries real
// policy numbers from the Rental Agreement and must not reach users until
// Jed's legal read-through clears it. Flip to true ONLY on that ruling.
export const A1_CONTRACT_LINE_CLEARED = false;
