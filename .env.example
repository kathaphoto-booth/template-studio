# Gemini AI — theme generation (optional; gallery works without it)
GEMINI_API_KEY="your-gemini-api-key"

# Supabase — selections + leads storage
# Project ref: hvvomiyskizxzhyytcfd
NEXT_PUBLIC_SUPABASE_URL="https://hvvomiyskizxzhyytcfd.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-key"
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"

# Resend — email notifications when a client picks a template
RESEND_API_KEY="your-resend-api-key"
NOTIFICATION_EMAIL="hello@kathabooth.com"

# Studio password gate (basic auth for the /admin studio)
STUDIO_PASSWORD="your-studio-password"

# App URL (set by Vercel at runtime; override for local dev)
APP_URL="http://localhost:3000"
