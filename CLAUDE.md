# photobooth-template-studio — Context Router

Standalone nested git repo (NOT a submodule). Next.js 15 + React 19 → Vercel,
serving book.kathabooth.com. Supabase project `hvvomiyskizxzhyytcfd`.

For directory-scoped context, read:
- Routing / pages / middleware: [app/CLAUDE.md](app/CLAUDE.md)
- UI components: [components/CLAUDE.md](components/CLAUDE.md)
- Business logic / DB / presets: [lib/CLAUDE.md](lib/CLAUDE.md)

Brand law, boot order, and HAM vault protocol live in the parent repo:
`/Users/jedg./Desktop/kat_ha_pb/CLAUDE.md` (vault at
`/Volumes/samsung 970 pro - Data/KATHA_VAULT/knowledge/.memory/`).

Hard rules: no legacy oax tokens, no forbidden hex (`#000`/`#fff`/`#F9F6F0`),
Loko Rust `#8C382A` is sacred-CTA-only, no `border-radius`, templates stay
symmetric (2/3/4 slots only). Verify with `npm run guard` before claiming done.
